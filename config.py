from datetime import timezone, timedelta

ICT           = timezone(timedelta(hours=7))
MARKET_OPEN   = (9, 0)
MARKET_CLOSE  = (15, 0)
TOP_N         = 10
OUTPUT_DIR    = "output"
HOSE_CODE     = "HSX"
